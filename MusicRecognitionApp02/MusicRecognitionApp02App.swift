import SwiftUI
import FirebaseCore

@main
struct SwiftUI_AuthApp: App {
    
    init() {
        FirebaseApp.configure()
    }
    
    var body: some Scene {
        WindowGroup {
            ContentView()
        }
    }
}
